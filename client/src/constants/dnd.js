// Custom MIME-shaped type so the browser preserves it verbatim. Deliberately
// distinct from KanbanBoard's legacy 'taskId' key, which lowercases to 'taskid'.
export const TASK_DRAG_TYPE = 'text/task-id'
